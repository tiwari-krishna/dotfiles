#!/bin/sh

SSID="$(nmcli -t -f active,ssid dev wifi | grep '^yes' | cut -c 5-)"
STRENGTH="$(nmcli -t -f SIGNAL device wifi list)"
#IP="$(curl -s https://ipinfo.io/ip)"

if [ "$SSID" = "" ]; then
        echo " 🔕 NC "
else
        echo "📶 ${STRENGTH}"
fi
